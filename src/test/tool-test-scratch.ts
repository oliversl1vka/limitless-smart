// Tool test scratch file — safe to delete
export function greet(name: string): string {
  return `Hello, ${name}!`;
}

export function goodbye(name: string): string {
  return `Goodbye, ${name}!`;
}

export const VERSION = "2.0.0";
