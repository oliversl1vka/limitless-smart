/** E2E multi-file test: math utilities */
export function square(n: number): number {
  return n * n;
}

export function cube(n: number): number {
  return n * n * n;
}

export function sqrt(n: number): number {
  return Math.sqrt(n);
}
